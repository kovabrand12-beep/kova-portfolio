node_modules/
.env
.DS_Store
npm-debug.log*

# Generated by `npm run build` — never edit by hand, never commit.
UPLOAD-TO-HOSTINGER/

# Booking submissions written by submit.php. Personal information: names,
# email addresses and IPs. Must never reach the repository.
leads/
kova-leads/

# SMTP credentials. Lives on the server only, never in the repo.
mail-config.php

# The upload package, rebuilt by `npm run build` whenever it is needed.
kova-site.zip
